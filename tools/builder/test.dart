// ignore_for_file: avoid_print

//import 'lib.dart';
//import 'package:markdown/markdown.dart';

//import '../extensions.dart';
//import 'builder.dart';

void main() {
  /*
  const markdown = '''
Foo *bar*
=========
''';
  final nodes = Document().parseLines(markdown);

  final result = TestCaseBuilder().build(nodes);

  print(result.map((e) => e.toMap()).toList().toPrettyString());
*/
}
