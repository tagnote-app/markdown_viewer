name: markdown_viewer
description: A Markdown renderer for Flutter. Render Markdown String to rich
  text output.
version: 0.4.1
repository: https://github.com/chenzhiguang/markdown_viewer

environment:
  sdk: '>=2.17.5 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  dart_markdown: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
